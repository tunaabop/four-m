<div class="row has-background-dark">
  <div class="col-auto has-background-dark">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4 has-background-dark">

<div>
  <h3> 0 Followers • 0 Following</h3>
</div>

  {{#if posts.length}}
  <div class="col-md-6 post-list has-background-dark">
    <h3>My Board:</h3>

    {{#each posts as |post|}}
    {{!-- <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.caption}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{post.id}}">DELETE</button>
      </div>
    </div> --}}
    <div class="card has-background-dark">
    <div class="card-image">
      <figure class="image is-48x48">
        {{!-- <img src="https://bulma.io/images/placeholders/1280x960.png" alt="Placeholder image"> --}}
        <a href="/post/{{post.post_id}}">{{{get_image post.image_path}}}</a>
      </figure>
    </div>
    <div class="card-content">
      <div class="media">
        {{!-- <div class="media-left">
          <figure class="image is-48x48">
            <img src="https://bulma.io/images/placeholders/96x96.png" alt="Placeholder image">
          </figure>
        </div> --}}
        <div class="media-content">
          <p class="title is-4">@{{post.user.username}}</p>
          {{!-- <p class="subtitle is-6">@johnsmith</p> --}}
        </div>
      </div>

      <div class="content">
        {{post.caption}}<a>@{{post.user.username}}</a>
        {{!-- <a href="#">#css</a> <a href="#">#responsive</a> --}}
        <br>
        <time datetime="{{format_date post.date_created}}">{{format_date post.date_created}}</time>
      </div>
    </div>
  </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<footer class="footer has-background-dark"> </footer>
<script src="/js/profile.js"></script>
